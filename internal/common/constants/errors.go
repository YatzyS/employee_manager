package constants

const (
	EmployeeNotFoundError = "employee with the given ID not found"
	InvalidIdError        = "invalid id passed in the request"
	NotEnoughDataError    = "not enough data in DB"
)
